/* Hide BoogaPlus navigation controls */
#bgpl_pos, #bgpl_navigate, #bgpl_selected {
    display: none !important;
}

/* Hide Gradio elements */
#bgpl_row {
    display: none !important;
}

/* Mobile navigation overlay */
.bgpl-nav-overlay {
    position: absolute;
    bottom: 10px;
    right: 10px;
    background: rgba(0, 0, 0, 0.7);
    padding: 15px;
    cursor: pointer;
    border-radius: 5px;
    display: none;
    z-index: 1000;
    min-width: 50px;
    text-align: center;
}

.bgpl-nav-overlay.active {
    display: block;
}

.bgpl-nav-container {
    position: absolute;
    display: none;
    z-index: 1000;
    width: 100%;
    padding: 5px;
    box-sizing: border-box;
    transition: all 0.2s ease;
}

.bgpl-nav-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(0, 0, 0, 0.7);
    border-radius: 5px;
    padding: 5px 10px;
    margin: 0 auto;
    max-width: 150px;
    gap: 10px;
    transition: all 0.2s ease, visibility 0.75s ease, opacity 0.75s ease !important;
}

.bgpl-nav-left,
.bgpl-nav-right {
    display: flex;
    align-items: center;
    color: white;
    cursor: pointer;
    padding: 4px;
    border-radius: 4px;
    transition: all 0.2s ease;
    min-width: 24px;
    justify-content: center;
}

.bgpl-nav-left:hover,
.bgpl-nav-right:hover {
    background: rgba(255, 255, 255, 0.1);
}

.bgpl-nav-pos {
    font-size: 12px;
    font-family: monospace;
    color: white;
    opacity: 0.9;
    min-width: 40px;
    text-align: center;
    user-select: none;
    flex: 1;
}

.bgpl-nav-arrow {
    font-size: 18px;
    opacity: 0.9;
    user-select: none;
}

.bgpl-nav-button {
    background: rgba(0, 0, 0, 0.6);
    color: white;
    border: none;
    padding: 15px;
    border-radius: 50%;
    cursor: pointer;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    transition: background 0.2s, transform 0.4s;
}

.bgpl-nav-button:hover {
    background: rgba(0, 0, 0, 0.8);
    transform: scale(1.1);
}

.bgpl-nav-button:active {
    transform: scale(0.95);
}

.bgpl-selected-msg {
    transition: all 0.3s;
    margin-left: -2px !important;
    position: relative;
}

.message:hover {
    background: rgba(0, 0, 0, 0.02);
}

.message, .message .message-body {
    transition: all 0.3s;
}

/* Hover effects */
.bgpl-nav-overlay:hover {
    background: rgba(0, 0, 0, 0.8);
}

.bgpl-nav-overlay:hover .bgpl-nav-info,
.bgpl-nav-overlay:hover .bgpl-nav-arrow {
    opacity: 1;
}

/* Mobile-specific adjustments */
@media (max-width: 768px) {
    .bgpl-nav-button {
        width: 44px;
        height: 44px;
        font-size: 20px;
    }
    
    .bgpl-nav-left {
        left: 5px;
    }
    
    .bgpl-nav-right {
        right: 5px;
    }
}