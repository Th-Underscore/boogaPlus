/* Hide Gradio elements */
#bgpl_info_row {
    display: none !important;
}

/* Mobile-compatible navigation overlay */
.message-info {
    display: none !important;
}

.boogaplus-container {
    position: absolute !important;
    display: flex;
    bottom: 5px;
    right: 5px;
    padding: 0px;
    justify-content: space-between;
    align-items: center;
    /* background: rgba(0, 0, 0, 1); */
    opacity: 1.0;
    border-radius: 6px;
    margin: 0 auto;
    max-width: 150px;
    max-height: 2em;
    height: 1.5em;
    transition: all 0.2s ease-in;
    overflow: hidden;
    /* Hover transition & transition-delay handled in JS */
}

.boogaplus-container[hidden] {
    display: none;
}

.nav-container {
    position: relative;
    display: flex;
    align-items: center;
    padding: 3px;
    border-radius: 6px !important;
    transition: all 0.2s ease-in;
    max-height: 100%;
    height: 1.3em;
    overflow: hidden;
}

.nav-container[hidden] {
    display: none;
}

.nav-arrow {
    font-size: 15px;
    opacity: 0.9;
    user-select: none;
    display: inline-block;
    color: white;
    opacity: 0.3;
    cursor: not-allowed;
    border-radius: 6px !important;
    transition: background 0.2s ease, opacity 0.2s ease;
    min-width: 20px;
    justify-content: center;
    padding: 0px;
    margin: 0px;
}

.nav-left {
    padding: 5px; /* 2px 2px 2px 5px; */
}

.nav-right {
    padding: 5px; /* 2px 5px 2px 2px; */
}

.nav-arrow[activated] {
    cursor: pointer;
    opacity: 1.0;
}

.nav-arrow[activated]:hover {
    background: rgba(255, 255, 255, 0.1);
    min-width: scale(1.1);
}

.nav-pos {
    display: inline-block;
    font-size: 11px;
    font-family: monospace;
    color: white;
    opacity: 0.9;
    min-width: 32px;
    text-align: center;
    user-select: none;
    padding: 0px;
    margin: 0px;
}

.message, .message .message-body {
    position: relative;
    transition: all 0.3s;
    will-change: transform !important;
}

.cai-chat .message:hover {
    background: rgba(0, 0, 0, 0.05);
}

.cai-chat .selected-message {
    transition: transform 0.3s ease-out;
    transform: translateX(-2px);
    transform-style: preserve-3d;
    backface-visibility: hidden;
    position: relative;
    width: 100%;
    box-sizing: border-box;
    animation: pulse 0.3s ease-in-out;
    animation-iteration-count: 1;
}

/* Mobile-specific adjustments */
/* @media (max-width: 768px) {
    .nav-button {
        width: 22px;
        height: 22px;
        font-size: 10px;
    }
    
    .nav-left {
        left: 5px;
    }
    
    .nav-right {
        right: 5px;
    }
} */